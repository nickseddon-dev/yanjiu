[project]
name = "adapters"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "common_schema",
    "pydantic>=2.0",
    "aiohttp>=3.9.0",
    "pandas>=2.0",
]

[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"
